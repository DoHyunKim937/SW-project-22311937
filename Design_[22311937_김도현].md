Design 
Simple Stock Indicator Viewer

# Contents

1. Introduction  
2. Class Diagram  
3. Sequence Diagram  
4. State Machine Diagram  
5. Implementation Requirements  
6. Glossary  
7. References

---

# 1. Introduction

본 문서는 Simple Stock Indicator Viewer 시스템의 Design 단계 문서이다.

요즘 세상에서 ai 시대가 오며 인공지능 기술 사업이 발달하며 주식 또한 엄청난 인기를 가지게 되었다. 현재 코스피가 사상 최대치를 갱신해가는 와중 많은 사람들이 주식에 관한 관심이 클텐데 이러한 사람들이 처음 주식을 보면 뭐가 무엇인지 모를 확률이 매우 크다. 그렇기에 그런 사람들을 돕고자 간단한 주식 관련 어플을 만들고자한다. 

Simple Stock Indicator Viewer는 초보 투자자가 기업의 주요 재무 지표를 쉽게 확인하고 이해할 수 있도록 돕는 웹 기반 소프트웨어이다. 사용자는 회원가입과 로그인을 거친 후 기업을 선택하고, 선택한 기업의 PER, ROE, PBR, PSR 등의 재무 지표를 조회할 수 있다. 또한 PER 값을 기준으로 저평가, 적정, 고평가와 같은 간단한 해석 결과를 확인할 수 있다.

분석 단계에서는 본 시스템의 주요 기능을 Register, Login, Select, Search, Analysis, Information, Comparison, Reset, Plus로 정리하였다. 디자인 단계에서는 이 기능들이 실제 구현 단계에서 어떤 클래스와 화면 흐름, 데이터 구조로 연결되는지 구체적으로 설계한다.

본 프로젝트는 실제 금융 API나 데이터베이스 서버를 사용하지 않는다. 구현 가능성을 고려하여 HTML, CSS, JavaScript를 사용하고, 기업 정보와 재무 지표는 로컬 JSON 파일에 미리 저장된 데이터를 사용한다. 회원가입과 로그인 기능은 학습용 프로젝트 수준에서 간단하게 구현한다.

이 문서의 목적은 구현 단계에서 참고할 클래스 구조, 기능별 동작 흐름, 상태 변화, 구현 요구사항을 정리하는 것이다.

---

# 2. Class Diagram

아래 이미지는 Simple Stock Indicator Viewer 시스템의 Class Diagram을 나타낸다.

<img width="1415" height="843" alt="스크린샷 2026-06-05 132902" src="https://github.com/user-attachments/assets/be1a182b-defc-4aa5-ae15-bde531c353f8" />


Class Diagram은 시스템을 구성하는 주요 클래스와 클래스 사이의 관계를 표현한다.  
본 시스템은 User가 MainSystem을 통해 여러 기능을 사용하는 구조이다. MainSystem은 전체 흐름을 관리하고, 사용자의 요청에 따라 회원가입, 로그인, 기업 선택, PER 조회, 지표 해석, 기업 비교, 추가 지표 조회, 초기화 기능을 각각 담당 클래스에 전달한다.

DataStorage는 사용자 정보와 기업 재무 데이터를 저장하고 제공하는 역할을 한다.  
Company는 기업 정보를 나타내고, FinancialIndicator는 해당 기업의 PER, ROE, PBR, PSR 값을 저장한다.

---

## 2.1 MainSystem

MainSystem 클래스는 시스템의 전체 실행 흐름을 관리하는 중심 클래스이다.  
사용자가 로그인한 뒤 기업 선택, 재무 지표 조회, 기업 비교, 데이터 초기화 등의 기능을 사용할 수 있도록 각 기능 클래스와 연결한다.

Attributes

```text
- currentUser : User
- selectedCompany : Company
- comparisonCompany : Company
```

Operations

```text
+ startSystem()
+ moveToLogin()
+ moveToRegister()
+ moveToMain()
+ selectCompany(companyId : String)
+ searchPER()
+ analyzePER()
+ showCompanyInfo()
+ compareCompanies()
+ searchAdditionalIndicators()
+ resetScreen()
```

Method Description

startSystem()  
시스템을 처음 실행할 때 호출되는 메소드이다.  
초기 화면을 준비하고 사용자가 로그인 화면에서 시작할 수 있도록 한다.

moveToLogin()  
현재 화면을 로그인 화면으로 전환하는 메소드이다.  
회원가입을 마친 뒤 다시 로그인 화면으로 돌아갈 때도 사용된다.

moveToRegister()  
사용자가 회원가입 버튼을 눌렀을 때 회원가입 화면으로 이동시키는 메소드이다.

moveToMain()  
로그인에 성공한 사용자를 메인 화면으로 이동시키는 메소드이다.  
메인 화면에서는 기업 선택, PER 조회, 추가 지표 조회, 기업 비교 등의 기능을 사용할 수 있다.

selectCompany(companyId : String)  
사용자가 선택한 기업 ID를 받아 해당 기업을 현재 선택된 기업으로 저장하는 메소드이다.  
저장된 기업 정보는 이후 PER 조회, 기업 정보 표시, 추가 지표 조회에서 사용된다.

searchPER()  
선택된 기업의 PER 값을 조회하기 위해 PERSearch 클래스를 호출하는 메소드이다.

analyzePER()  
조회된 PER 값을 기준으로 해석 결과를 생성하기 위해 IndicatorAnalysis 클래스를 호출하는 메소드이다.

showCompanyInfo()  
선택된 기업의 기본 정보를 화면에 표시하기 위해 CompanyInformation 클래스를 호출하는 메소드이다.

compareCompanies()  
두 개 이상의 기업을 비교하기 위해 CompanyComparison 클래스를 호출하는 메소드이다.

searchAdditionalIndicators()  
PER 외에 ROE, PBR, PSR 값을 조회하기 위해 AdditionalIndicatorSearch 클래스를 호출하는 메소드이다.

resetScreen()  
화면에 표시된 기업 정보, 재무 지표, 해석 결과, 비교 결과를 초기화하기 위해 ResetManager 클래스를 호출하는 메소드이다.

---

## 2.2 User

User 클래스는 시스템을 사용하는 일반 사용자를 나타낸다.  
사용자는 회원가입을 통해 ID, password, email을 등록하고, 로그인 이후 시스템의 주요 기능을 사용할 수 있다.

Attributes

```text
- userId : String
- password : String
- email : String
- loginStatus : boolean
```

Operations

```text
+ getUserId()
+ getPassword()
+ getEmail()
+ isLoggedIn()
+ setLoginStatus(status : boolean)
```

Method Description

getUserId()  
사용자의 ID 값을 반환하는 메소드이다.  
로그인 확인이나 사용자 정보 조회 과정에서 사용된다.

getPassword()  
사용자의 password 값을 반환하는 메소드이다.  
로그인 과정에서 입력한 password와 저장된 password를 비교할 때 사용된다.

getEmail()  
사용자의 email 값을 반환하는 메소드이다.  
회원가입 정보 확인이나 사용자 정보 관리 과정에서 사용된다.

isLoggedIn()  
현재 사용자가 로그인 상태인지 확인하는 메소드이다.  
로그인 상태가 true인 경우에만 기업 조회 기능을 사용할 수 있다.

setLoginStatus(status : boolean)  
사용자의 로그인 상태를 변경하는 메소드이다.  
로그인 성공 시 true로 변경하고, 로그아웃이나 초기 상태에서는 false로 변경할 수 있다.

---

## 2.3 Registration

Registration 클래스는 회원가입 기능을 담당한다.  
사용자가 ID, password, email을 입력하면 입력값이 비어 있는지 확인하고, 이미 존재하는 ID인지 검사한다.

Attributes

```text
- inputId : String
- inputPassword : String
- inputEmail : String
```

Operations

```text
+ validateInput(id : String, password : String, email : String)
+ checkDuplicateId(id : String)
+ registerUser(user : User)
```

Method Description

validateInput(id : String, password : String, email : String)  
회원가입 화면에서 입력된 ID, password, email 값이 비어 있는지 확인하는 메소드이다.  
필수 정보가 입력되지 않은 경우 회원가입을 진행하지 않고 오류 메시지를 표시한다.

checkDuplicateId(id : String)  
입력한 ID가 이미 등록된 ID인지 확인하는 메소드이다.  
DataStorage에 저장된 사용자 정보와 비교하여 중복 여부를 판단한다.

registerUser(user : User)  
회원가입 조건을 만족한 사용자 정보를 저장하는 메소드이다.  
입력값 검증과 ID 중복 확인이 끝난 뒤 DataStorage에 User 정보를 저장한다.

---

## 2.4 Login

Login 클래스는 사용자가 시스템에 접근할 수 있는지 확인한다.  
사용자가 입력한 ID와 password를 저장된 사용자 정보와 비교하여 로그인 성공 또는 실패 여부를 판단한다.

Attributes

```text
- inputId : String
- inputPassword : String
```

Operations

```text
+ loginCheck(id : String, password : String)
+ showLoginError()
+ showLoginSuccess()
```

Method Description

loginCheck(id : String, password : String)  
사용자가 입력한 ID와 password가 저장된 사용자 정보와 일치하는지 확인하는 메소드이다.  
정보가 일치하면 로그인 성공으로 처리하고, 일치하지 않으면 로그인 실패로 처리한다.

showLoginError()  
로그인에 실패했을 때 오류 메시지를 화면에 표시하는 메소드이다.  
ID가 존재하지 않거나 password가 일치하지 않을 때 실행된다.

showLoginSuccess()  
로그인에 성공했을 때 성공 결과를 처리하는 메소드이다.  
사용자의 로그인 상태를 변경하고 메인 화면으로 이동할 수 있도록 한다.

---

## 2.5 CompanySelector

CompanySelector 클래스는 사용자가 조회하고자 하는 기업을 선택하는 기능을 담당한다.  
시스템은 기업 목록을 보여주고, 사용자가 선택한 기업 정보를 저장한다.

Attributes

```text
- companyList : Company[]
- selectedCompanyId : String
```

Operations

```text
+ loadCompanyList()
+ selectCompany(companyId : String)
+ getSelectedCompany()
```

Method Description

loadCompanyList()  
DataStorage에 저장된 기업 목록을 불러오는 메소드이다.  
사용자가 선택할 수 있는 기업 목록을 화면에 표시하기 위해 사용된다.

selectCompany(companyId : String)  
사용자가 선택한 기업 ID를 저장하는 메소드이다.  
선택된 기업은 PER 조회, 기업 정보 표시, 추가 지표 조회, 기업 비교 기능에서 사용된다.

getSelectedCompany()  
현재 선택된 기업 정보를 반환하는 메소드이다.  
다른 기능 클래스가 선택된 기업 정보를 사용할 때 호출된다.

---

## 2.6 Company

Company 클래스는 재무 지표 조회 대상이 되는 기업 정보를 나타낸다.

Attributes

```text
- companyId : String
- companyName : String
- industry : String
- description : String
- indicator : FinancialIndicator
```

Operations

```text
+ getCompanyId()
+ getCompanyName()
+ getIndustry()
+ getDescription()
+ getIndicator()
```

Method Description

getCompanyId()  
기업의 고유 ID를 반환하는 메소드이다.  
기업을 구분하거나 선택한 기업 데이터를 찾을 때 사용된다.

getCompanyName()  
기업명을 반환하는 메소드이다.  
기업 정보 화면이나 비교 결과 화면에 기업명을 표시할 때 사용된다.

getIndustry()  
기업의 업종 정보를 반환하는 메소드이다.  
사용자가 선택한 기업이 어떤 분야에 속하는지 보여줄 때 사용된다.

getDescription()  
기업의 주요 사업 설명을 반환하는 메소드이다.  
기업 정보 표시 기능에서 사용된다.

getIndicator()  
기업이 가지고 있는 FinancialIndicator 객체를 반환하는 메소드이다.  
PER, ROE, PBR, PSR 값을 조회할 때 사용된다.

---

## 2.7 CompanyInformation

CompanyInformation 클래스는 선택한 기업의 기본 정보를 화면에 표시한다.

Attributes

```text
- company : Company
```

Operations

```text
+ showCompanyName()
+ showIndustry()
+ showDescription()
+ displayCompanyInfo()
```

Method Description

showCompanyName()  
선택한 기업의 이름을 화면에 표시하는 메소드이다.

showIndustry()  
선택한 기업의 업종을 화면에 표시하는 메소드이다.

showDescription()  
선택한 기업의 주요 사업 설명을 화면에 표시하는 메소드이다.

displayCompanyInfo()  
기업명, 업종, 주요 사업 설명을 한 번에 화면에 출력하는 메소드이다.  
사용자가 기업을 선택했을 때 기업의 기본 정보를 확인할 수 있도록 한다.

---

## 2.8 FinancialIndicator

FinancialIndicator 클래스는 기업의 재무 지표 정보를 저장한다.

Attributes

```text
- per : double
- roe : double
- pbr : double
- psr : double
```

Operations

```text
+ getPER()
+ getROE()
+ getPBR()
+ getPSR()
```

Method Description

getPER()  
기업의 PER 값을 반환하는 메소드이다.  
PER 조회 기능과 PER 해석 기능에서 사용된다.

getROE()  
기업의 ROE 값을 반환하는 메소드이다.  
추가 지표 조회 기능에서 기업의 수익성을 보여줄 때 사용된다.

getPBR()  
기업의 PBR 값을 반환하는 메소드이다.  
추가 지표 조회 기능에서 자산 대비 주가 수준을 보여줄 때 사용된다.

getPSR()  
기업의 PSR 값을 반환하는 메소드이다.  
추가 지표 조회 기능에서 매출 대비 주가 수준을 보여줄 때 사용된다.

---

## 2.9 PERSearch

PERSearch 클래스는 선택한 기업의 PER 값을 조회한다.

Attributes

```text
- company : Company
- perValue : double
```

Operations

```text
+ searchPER(company : Company)
+ displayPER(per : double)
+ showPERError()
```

Method Description

searchPER(company : Company)  
선택된 기업의 FinancialIndicator에서 PER 값을 가져오는 메소드이다.  
기업이 선택되어 있지 않거나 PER 값이 없으면 오류 처리가 필요하다.

displayPER(per : double)  
조회된 PER 값을 화면에 표시하는 메소드이다.  
사용자는 이 메소드를 통해 선택한 기업의 PER 값을 확인할 수 있다.

showPERError()  
PER 조회가 불가능할 때 오류 메시지를 표시하는 메소드이다.  
기업이 선택되지 않았거나 PER 데이터가 존재하지 않을 때 사용된다.

---

## 2.10 IndicatorAnalysis

IndicatorAnalysis 클래스는 PER 값을 기준으로 기업의 평가 상태를 해석한다.

Attributes

```text
- perValue : double
- analysisResult : String
```

Operations

```text
+ analyzePER(per : double)
+ createAnalysisText()
+ displayAnalysisResult()
```

Method Description

analyzePER(per : double)  
PER 값을 기준으로 저평가, 적정, 고평가 여부를 판단하는 메소드이다.  
본 프로젝트에서는 단순 기준을 사용하여 PER 값이 낮으면 저평가 가능성, 중간이면 적정 수준, 높으면 고평가 가능성으로 분류한다.

createAnalysisText()  
분석 결과를 사용자가 이해하기 쉬운 문장으로 변환하는 메소드이다.  
예를 들어 “PER 기준으로 볼 때 적정한 평가를 받고 있는 것으로 볼 수 있다.”와 같은 설명을 생성한다.

displayAnalysisResult()  
생성된 해석 결과를 화면에 표시하는 메소드이다.  
초보 투자자가 PER 값의 의미를 쉽게 이해할 수 있도록 돕는다.

---

## 2.11 CompanyComparison

CompanyComparison 클래스는 두 개 이상의 기업을 비교한다.

Attributes

```text
- firstCompany : Company
- secondCompany : Company
- comparisonResult : String
```

Operations

```text
+ comparePER(first : Company, second : Company)
+ compareIndicators(first : Company, second : Company)
+ displayComparisonResult()
```

Method Description

comparePER(first : Company, second : Company)  
두 기업의 PER 값을 비교하는 메소드이다.  
초기 버전에서는 PER을 중심으로 어느 기업이 상대적으로 낮거나 높은 평가를 받고 있는지 보여준다.

compareIndicators(first : Company, second : Company)  
두 기업의 여러 재무 지표를 비교하는 메소드이다.  
PER 외에도 ROE, PBR, PSR 비교로 확장할 수 있다.

displayComparisonResult()  
기업 비교 결과를 화면에 표시하는 메소드이다.  
비교 대상 기업의 지표와 간단한 해석을 함께 보여준다.

---

## 2.12 AdditionalIndicatorSearch

AdditionalIndicatorSearch 클래스는 PER 외에 ROE, PBR, PSR 값을 조회한다.

Attributes

```text
- company : Company
- roeValue : double
- pbrValue : double
- psrValue : double
```

Operations

```text
+ searchROE(company : Company)
+ searchPBR(company : Company)
+ searchPSR(company : Company)
+ displayAdditionalIndicators()
```

Method Description

searchROE(company : Company)  
선택한 기업의 ROE 값을 조회하는 메소드이다.  
ROE는 기업의 수익성을 확인하는 데 사용된다.

searchPBR(company : Company)  
선택한 기업의 PBR 값을 조회하는 메소드이다.  
PBR은 기업의 자산 대비 주가 수준을 확인하는 데 사용된다.

searchPSR(company : Company)  
선택한 기업의 PSR 값을 조회하는 메소드이다.  
PSR은 기업의 매출 대비 주가 수준을 확인하는 데 사용된다.

displayAdditionalIndicators()  
ROE, PBR, PSR 값을 화면에 표시하는 메소드이다.  
사용자가 PER 외의 지표도 함께 확인할 수 있도록 한다.

---

## 2.13 ResetManager

ResetManager 클래스는 화면에 표시된 선택 정보와 조회 결과를 초기 상태로 되돌린다.

Attributes

```text
- resetStatus : boolean
```

Operations

```text
+ clearSelectedCompany()
+ clearIndicatorResult()
+ clearAnalysisResult()
+ clearComparisonResult()
+ resetScreen()
```

Method Description

clearSelectedCompany()  
현재 선택된 기업 정보를 제거하는 메소드이다.  
사용자가 다시 기업을 선택할 수 있도록 초기 상태로 만든다.

clearIndicatorResult()  
화면에 표시된 PER, ROE, PBR, PSR 값을 제거하는 메소드이다.

clearAnalysisResult()  
화면에 표시된 PER 해석 결과를 제거하는 메소드이다.

clearComparisonResult()  
화면에 표시된 기업 비교 결과를 제거하는 메소드이다.

resetScreen()  
선택 기업, 재무 지표, 해석 결과, 비교 결과를 모두 초기화하는 메소드이다.  
단, 사용자 계정 정보 자체는 삭제하지 않는다.

---

## 2.14 DataStorage

DataStorage 클래스는 사용자 정보와 기업 재무 데이터를 저장하고 제공한다.

Attributes

```text
- userData : User[]
- companyData : Company[]
```

Operations

```text
+ saveUser(user : User)
+ findUserById(id : String)
+ validateUser(id : String, password : String)
+ loadCompanyData()
+ findCompanyById(companyId : String)
```

Method Description

saveUser(user : User)  
회원가입이 완료된 사용자 정보를 저장하는 메소드이다.  
본 프로젝트에서는 학습용으로 localStorage에 사용자 정보를 저장할 수 있다.

findUserById(id : String)  
입력된 ID와 일치하는 사용자가 존재하는지 찾는 메소드이다.  
회원가입 시 ID 중복 확인과 로그인 시 사용자 조회에 사용된다.

validateUser(id : String, password : String)  
입력된 ID와 password가 저장된 사용자 정보와 일치하는지 확인하는 메소드이다.  
로그인 성공 또는 실패 여부를 판단할 때 사용된다.

loadCompanyData()  
로컬 JSON 파일에 저장된 기업 데이터를 불러오는 메소드이다.  
기업 목록을 화면에 표시하거나 기업 정보를 조회할 때 사용된다.

findCompanyById(companyId : String)  
기업 ID를 기준으로 특정 기업 데이터를 찾는 메소드이다.  
기업 선택, PER 조회, 기업 정보 표시, 추가 지표 조회 기능에서 사용된다.

본 프로젝트에서는 실제 서버와 데이터베이스를 사용하지 않는다.  
기업 정보와 재무 지표는 로컬 JSON 데이터에 저장하고, 회원가입 및 로그인 정보는 학습용으로 브라우저의 localStorage를 활용할 수 있다.


---

# 3. Sequence Diagram

Sequence Diagram은 기능이 실행될 때 객체 사이에서 어떤 메시지가 오가는지 시간 순서대로 표현한다.  
본 프로젝트에서는 중심 기능을 기준으로 다섯 개의 Sequence Diagram을 작성한다.

---

## 3.1 Login Sequence Diagram

<img width="1138" height="725" alt="스크린샷 2026-06-05 143045" src="https://github.com/user-attachments/assets/0a9311b1-35c0-4d93-8ce7-318cebec5980" />


로그인 기능은 사용자가 ID와 password를 입력할 때 시작된다.  
LoginUI는 입력된 정보를 MainSystem에 전달하고, MainSystem은 Login 클래스에 로그인 확인을 요청한다.  
Login 클래스는 DataStorage에 저장된 사용자 정보와 입력값을 비교한다.  
정보가 일치하면 메인 화면으로 이동하고, 일치하지 않으면 로그인 실패 메시지를 표시한다.

---

## 3.2 Register Sequence Diagram

<img width="1132" height="682" alt="스크린샷 2026-06-05 144932" src="https://github.com/user-attachments/assets/cbaabdd9-4c4f-4454-a55c-af037004b492" />


회원가입 기능은 사용자가 ID, password, email을 입력할 때 시작된다.  
RegisterUI는 입력 정보를 MainSystem에 전달하고, MainSystem은 Registration 클래스에 회원가입 처리를 요청한다.  
Registration 클래스는 입력값을 확인하고 ID 중복 여부를 검사한다.  
조건을 만족하면 사용자 정보를 저장하고, 회원가입이 완료되었다는 메시지를 표시한다.

---

## 3.3 PER Search Sequence Diagram

<img width="1210" height="720" alt="스크린샷 2026-06-05 143721" src="https://github.com/user-attachments/assets/45a0ecb2-4274-49c7-9bb6-61d0076d3b6a" />


PER 조회 기능은 사용자가 기업을 선택한 후 PER 조회 버튼을 누를 때 실행된다.  
MainSystem은 PERSearch 클래스에 조회를 요청하고, PERSearch는 선택된 Company의 FinancialIndicator에서 PER 값을 가져온다.  
조회된 PER 값은 사용자 화면에 표시된다.

---

## 3.4 Additional Indicator Sequence Diagram

<img width="1197" height="696" alt="스크린샷 2026-06-05 145511" src="https://github.com/user-attachments/assets/66e22401-8c5b-4732-b330-b2d6b3d74b81" />



추가 지표 조회 기능은 사용자가 추가 지표 조회 버튼을 누를 때 실행된다.  
AdditionalIndicatorSearch 클래스는 선택한 기업의 FinancialIndicator에서 ROE, PBR, PSR 값을 가져온다.  
조회된 추가 지표는 화면에 표시되어 사용자가 기업의 수익성, 자산 대비 평가 수준, 매출 대비 주가 수준을 함께 확인할 수 있도록 한다.

---

## 3.5 Company Comparison Sequence Diagram

<img width="1187" height="657" alt="image" src="https://github.com/user-attachments/assets/1150df7f-cee5-4977-b4c9-f2d873dfca32" />


기업 비교 기능은 사용자가 두 개 이상의 기업을 선택한 후 기업 비교 버튼을 누를 때 실행된다.  
CompanyComparison 클래스는 선택된 기업들의 FinancialIndicator에서 PER 값을 가져와 비교한다.  
비교 결과는 사용자 화면에 표시된다.

---

# 4. State Machine Diagram

아래 이미지는 Simple Stock Indicator Viewer 시스템의 State Machine Diagram을 나타낸다.

이미지 칸

State Machine Diagram은 시스템이 실행된 후 사용자의 입력에 따라 어떤 상태로 이동하는지 표현한다.  
본 시스템은 Launch System 상태에서 시작하여 Login Screen으로 이동한다. 사용자는 회원가입을 진행하거나 로그인할 수 있다. 로그인에 성공하면 Main Screen으로 이동하고, 기업 선택 이후 재무 지표 조회, 추가 지표 조회, 기업 비교, 초기화 기능을 사용할 수 있다.

---

## 4.1 State Description

Launch System

웹 브라우저에서 index.html 파일을 열어 시스템이 실행된 상태이다.

Login Screen

사용자가 ID와 password를 입력하여 로그인할 수 있는 상태이다.  
로그인에 성공하면 Main Screen으로 이동하고, 실패하면 Error Message 상태로 이동한다.

Register Screen

사용자가 ID, password, email을 입력하여 회원가입을 수행하는 상태이다.  
회원가입이 성공하면 Login Screen으로 돌아간다.

Main Screen

로그인에 성공한 사용자가 시스템의 주요 기능을 사용할 수 있는 상태이다.  
기업 선택, PER 조회, 추가 지표 조회, 기업 비교, 초기화 기능을 실행할 수 있다.

Company Selected

사용자가 기업 목록에서 기업을 선택한 상태이다.  
선택된 기업은 이후 PER 조회, 기업 정보 표시, 추가 지표 조회, 기업 비교 기능에서 사용된다.

Show Company Information

선택한 기업의 기업명, 업종, 주요 사업 설명을 보여주는 상태이다.

Show PER Result

선택한 기업의 PER 값을 화면에 표시하는 상태이다.

Show Analysis Result

PER 값을 기준으로 저평가, 적정, 고평가 등의 해석 결과를 보여주는 상태이다.

Show Additional Indicators

선택한 기업의 ROE, PBR, PSR 값을 화면에 표시하는 상태이다.

Show Comparison Result

두 개 이상의 기업을 선택하여 PER 중심 비교 결과를 보여주는 상태이다.

Reset Screen

선택된 기업 정보, PER 값, 추가 지표, 해석 결과, 비교 결과를 초기 상태로 되돌리는 상태이다.

Error Message

사용자가 잘못된 입력을 하거나 필요한 조건을 만족하지 못했을 때 오류 메시지를 보여주는 상태이다.

End

사용자가 시스템 사용을 종료한 상태이다.

---

# 5. Implementation Requirements

Simple Stock Indicator Viewer 시스템을 구현하기 위해 필요한 요구사항은 다음과 같다.

---

## 5.1 Hardware Requirements

CPU

```text
Intel i3 이상 또는 동급 성능의 CPU
```

RAM

```text
4GB 이상
```

Storage

```text
100MB 이상의 여유 공간
```

Network

```text
실시간 금융 API를 사용하지 않으므로 실행 자체에는 네트워크 연결이 필수 사항이 아니다.
다만 GitHub 저장소 접근, 자료 조사등을 위해 인터넷 연결이 필요할 수 있다.
```

---

## 5.2 Software Requirements

Operating System

```text
Windows 10 이상 또는 macOS, Linux 환경
```

Web Browser

```text
Chrome, Edge, Firefox 등 최신 웹 브라우저
```

Implementation Language

```text
HTML
CSS
JavaScript
```

Data Format

```text
JSON
```

Development Tool

```text
Visual Studio Code
GitHub
StarUML
```

---
## 5.3 Nonfunctional Requirements

Usability

```text
초보 투자자도 쉽게 이해할 수 있도록 화면 구성을 단순하고 직관적으로 설계한다.
```

Performance

```text
로컬 데이터를 사용하므로 일반적인 조회 결과는 1초 이내에 표시되도록 한다.
```

Maintainability

```text
HTML, CSS, JavaScript 파일을 분리하여 유지보수하기 쉽게 구성한다.
```

Data Limitation

```text
본 프로젝트는 실시간 금융 API를 사용하지 않고, 미리 저장된 로컬 JSON 데이터를 기반으로 동작한다.
```

Security Limitation

```text
회원가입과 로그인 기능은 학습 목적의 간단한 방식으로 구현한다.
실제 서비스에서는 비밀번호 암호화와 서버 기반 인증이 필요하다.
```

Reliability

```text
기업이 선택되지 않았거나 데이터가 존재하지 않는 경우 오류 메시지를 제공한다.
```

---

# 6. Glossary

Class Diagram

시스템을 구성하는 클래스와 클래스 사이의 관계를 표현한 다이어그램이다.

Sequence Diagram

기능이 실행될 때 객체 사이에서 어떤 메시지가 오가는지 시간 순서대로 표현한 다이어그램이다.

State Machine Diagram

시스템이 실행되는 동안 어떤 상태를 거치며 변화하는지 표현한 다이어그램이다.

HTML

웹페이지의 구조를 만드는 언어이다.

CSS

웹페이지의 디자인을 담당하는 스타일 언어이다.

JavaScript

웹페이지의 동작을 구현하는 프로그래밍 언어이다.

JSON

데이터를 저장하고 전달하기 위한 텍스트 기반 데이터 형식이다.

localStorage

웹 브라우저에 데이터를 저장할 수 있는 저장 공간이다.

PER

Price-to-Earnings Ratio의 약자이다.  
주가를 주당순이익으로 나눈 값으로 기업의 가치 평가에 사용된다.

ROE

Return on Equity의 약자이다.  
자기자본 대비 순이익의 비율로 기업의 수익성을 나타낸다.

PBR

Price-to-Book Ratio의 약자이다.  
주가를 주당순자산으로 나눈 값으로 기업의 자산 대비 평가 수준을 나타낸다.

PSR

Price-to-Sales Ratio의 약자이다.  
주가를 매출액으로 나눈 값으로 기업의 매출 대비 주가 수준을 나타낸다.

Method

클래스 내부에서 특정 기능을 수행하는 함수이다.

Attribute

클래스가 가지고 있는 데이터나 속성이다.

---
